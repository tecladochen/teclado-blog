---
import { themeConfig } from '~/.config'
import { getPathFromSeries } from '~/utils'

interface Props {
  name: string
}

const { name } = Astro.props
const { translate: t } = Astro.locals

const href = `/series/${getPathFromSeries(name, themeConfig.site.seriesMap)}`
---

<a href={href}>
  {t('in_series')}: {name}
</a>
