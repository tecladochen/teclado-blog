---
import katexStylesheet from 'katex/dist/katex.min.css?url'
import { themeConfig } from '~/.config'

const { latex } = themeConfig

const katex = latex.katex
---

{
  katex && <link rel="stylesheet" href={katexStylesheet} />
}
