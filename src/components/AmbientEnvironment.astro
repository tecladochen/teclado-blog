<div class="ambient-environment" data-ambient-environment data-mode="none">
  <canvas class="ambient-canvas" data-ambient-canvas aria-hidden="true"></canvas>

  <button class="ambient-trigger" type="button" data-ambient-trigger aria-label="切换环境动效">
    <span class="ambient-icon-track" aria-hidden="true">
      <span class="i-tabler-flower-off"></span>
      <span class="i-tabler-flower"></span>
      <span class="i-tabler-flower-off"></span>
    </span>
  </button>
</div>

<script>
  import { setupAmbientEnvironment } from '~/scripts/ambientEnvironment'

  setupAmbientEnvironment()
</script>

<style>
  .ambient-environment {
    --ambient-index: 0;
    position: relative;
    z-index: 35;
    display: flex;
    align-items: center;
  }

  .ambient-canvas {
    position: fixed;
    inset: 0;
    z-index: 30;
    width: 100vw;
    height: 100vh;
    pointer-events: none;
    opacity: 1;
    transition: opacity 160ms ease;
  }

  .ambient-canvas.is-switching {
    opacity: 0;
  }

  .ambient-trigger {
    display: block;
    width: 32px;
    height: 30px;
    padding: 0;
    overflow: hidden;
    border: 0;
    border-radius: 10px;
    background: transparent;
    color: #9b9b97;
    cursor: pointer;
    transition:
      color 220ms ease,
      background 220ms ease,
      box-shadow 220ms ease,
      transform 300ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .ambient-trigger:hover {
    background: rgba(38, 38, 36, 0.05);
    color: #555550;
    transform: translateY(-1px);
  }

  .ambient-environment[data-mode='petals'] .ambient-trigger {
    background: rgba(154, 104, 96, 0.09);
    color: #986961;
    box-shadow: inset 0 0 0 1px rgba(154, 104, 96, 0.08);
  }

  .ambient-environment[data-mode='petals'] .ambient-trigger:hover {
    background: rgba(154, 104, 96, 0.14);
    color: #82584f;
  }

  .ambient-trigger:active {
    transform: translateY(0) scale(0.94);
  }

  .ambient-trigger:disabled {
    cursor: not-allowed;
    opacity: 0.36;
  }

  .ambient-icon-track {
    display: flex;
    width: 90px;
    height: 30px;
    margin-left: 1px;
    align-items: center;
    transform: translateX(calc(var(--ambient-index) * -30px));
    transition: transform 440ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .ambient-environment.is-snapping .ambient-icon-track {
    transition: none;
  }

  .ambient-icon-track > span {
    display: block;
    flex: 0 0 30px;
    width: 30px;
    height: 30px;
    opacity: 0.88;
    transform: scale(0.54);
    transition:
      opacity 220ms ease,
      transform 440ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .ambient-environment[data-mode='petals'] .ambient-icon-track > span {
    opacity: 1;
    transform: scale(0.6) rotate(8deg);
  }

  :global(.dark) .ambient-trigger {
    color: #737373;
  }

  :global(.dark) .ambient-trigger:hover {
    background: rgba(255, 255, 255, 0.07);
    color: #e5e5e5;
  }

  :global(.dark) .ambient-environment[data-mode='petals'] .ambient-trigger {
    background: rgba(213, 174, 166, 0.1);
    color: #c79a92;
    box-shadow: inset 0 0 0 1px rgba(213, 174, 166, 0.08);
  }

  @media (max-width: 639px) {
    .ambient-environment {
      margin-left: -2px;
    }

    .ambient-trigger {
      width: 28px;
      height: 28px;
    }

    .ambient-icon-track {
      height: 28px;
      margin-left: 0;
      transform: translateX(calc(var(--ambient-index) * -30px - 1px));
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ambient-canvas {
      display: none;
    }

    .ambient-trigger,
    .ambient-icon-track {
      transition: none;
    }
  }
</style>
